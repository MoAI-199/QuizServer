using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace COMMON{
    public enum FASE {
        START,
        CONNECT,
        CHOICE,
        ANSWER,
        NONE
    }
    public enum SCENE{
        TITLE,
        GAME,
        RESULT,
    }
}